# Car Ads Next.js MVP 🚗

## Quick Start
1. Make sure you have Node.js installed on your machine.
2. Open your terminal (Cmd, PowerShell, or within VS Code) in this directory (`Car_Advertisemeng`).
3. Run `npm install` to install all dependencies.
4. Run `npm run dev` to start the local development server.
5. Open `http://localhost:3000` in your browser.

## Tech Stack Used
* Next.js (App Directory Routing)
* Tailwind CSS + Lucide Icons
* TypeScript

## Phases Completed (from your brief)
✅ MVP Structure and routing (Home, Services, Portfolio, Case Studies, Pricing, Contact)
✅ Admin panel scaffolded (`/admin`)
✅ Contact Form UI & WhatsApp link integration
✅ SEO + Desktop/Mobile Responsive
✅ Dark Theme with Accents (`#0B0B0B` Primary, `#FFD60A` Accent)

## Phase 3 Integration (Pending Setup by you):
* MongoDB & Mongoose schemas are conceptualized (Mongoose included in `package.json`).
* Connect via `/api/leads` and `/api/campaigns` files once Atlas URI is ready.
